export const updateCours = (state, cours) => {
    state.cours = cours
}

export const SET_COURS_ACTUEL = (state, cours) => {
    state.coursActuel = cours
}

export const SET_CHAPITRES = (state, chapitres) => {
    state.chapitres = chapitres
}

export const SET_SECTIONS = (state, sections) => {
    state.sections = sections
}

export const SET_INDICE_SECTION_COURANTE = (state, indice) => {
    state.indiceSectionCourante = indice
}
